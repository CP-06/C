#include<stdio.h>
int main()
{
    int a;
    printf("Enter the value you want to display : "); 
    scanf("%d", &a); // scanf is known as the read function
    printf("The value of a is %d", a); // printf is known as the write function
    return 0;
}