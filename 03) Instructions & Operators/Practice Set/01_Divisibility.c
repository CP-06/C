/*
Write a program to check whether a number is divisible by 97 or not. 
*/

#include <stdio.h>
int main()
{
    int a;
    printf("Enter the value : ");
    scanf("%d", &a);
    printf("The value after division is %d", a%97);
    return 0;
}